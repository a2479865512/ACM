/*
RSA
取两素数p q
n = p *q
Φ(n)=n*Π因子(1-1/i)
Φ(n)=Φ(p*q)=Φ(p)*Φ(q)=(p-1)*(q-1)

随机公钥 1< e < Φ(n) 且  gcd(e,Φ(n)) = 1

密钥 d = e^-1 % Φ(n)

因为 x^y % n = x^(y % Φ(n)) %n
特别的 有 x^0 % n = 1 = x^Φ(n) % n = x * x^ (Φ(n)-1) % n= x * x^-1 %n

d = e^-1 = e ^ ( Φ( Φ(n) ) - 1 ) 不过这里还是扩展欧几里得快 log(Φ(n))

加密 C = M^e % n
解密 M = C^d % n = M^(e * d) % n =M % n

*/